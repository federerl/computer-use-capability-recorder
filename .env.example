# Discovery only. Replay and the test suite run without this.
ANTHROPIC_API_KEY=sk-ant-...

# Target app operator credentials. Never committed, never written into artifacts.
BACKOFFICE_USER=roper
BACKOFFICE_PASSWORD=change-me-locally

# Target app
BASE_URL=http://127.0.0.1:5000
TENANT=meridian
FAULTS=on
